//centralize router by setting router up in here
//but placing defautls inside of index.js(main BE file)
const primaryServer = require('express').Router()


//ROUTES

// primaryServer.use('/projects',projects)
// primaryServer.use('/',)
// primaryServer.use('',)


module.exports = primaryServer